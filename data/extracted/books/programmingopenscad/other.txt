                                                         Programming with OpenSCAD
                                                   A Beginner’s Guide to Coding 3D-Printable Objects
                                                                          by Justin Gohde and Marius Kintel

                                                                                Errata updated to print 2
                                                                                                                                                                                 Print
Page                                          Error                                                                            Correction
                                                                                                                                                                               corrected

11     The cylinder shown in Figure 1-13, for example, uses only six line segments to      The cylinder shown in Figure 1-13, for example, uses only seven line segments to    Print 2
       approximate the curve of the circular faces of the cylinder.                        approximate the curve of the circular faces of the cylinder.

19     Especially complex designs require more surfaces and might have slow Render times   Especially complex designs require more surfaces and might have slow render times   Print 2
       as a result.                                                                        as a result.

99     4.     +, -                                                                         4.   +, -, !                                                                        Print 2

103                                                                                                                                                                            Print 2
        } else {                                                                            } else {
          if (y == 10) {                                                                      if (y == 10) {
            // code that is executed only when both x >= 8 and y ==10                           // code that is executed only when both x >= 8 and y == 10
          }                                                                                   }
        else {                                                                              else {
          // code that is executed only when both x >= 8 and y !=10                           // code that is executed only when both x >= 8 and y != 10
          }                                                                                   }
        }                                                                                   }


134–                                                                                                                                                                           Print 2
135     module ring(inner_radius, outer_radius, height) {                                   module ring(inner_radius, outer_radius, height) {
            difference() {                                                                      difference() {
                cylinder(h=height, r=outer_radius);                                                 cylinder(h=height, r=outer_radius);
                translate([0, 0, 1])                                                                translate([0, 0, -1])
                cylinder(h=height+2, r=inner_radius, center=true);                                  cylinder(h=height+2, r=inner_radius, center=true);


157    +, -                                                                                +, -, !                                                                             Print 2
