                                                                                  Python One-Liners
                                                                    Write Concise, Eloquent Python Like a Professional

                                                                                        by Christian Mayer

                                                                                     Errata updated to print 3
                                                                                                                                                                                          Print
Page                                             Error                                                                                Correction
                                                                                                                                                                                        corrected

5      Instead, Python uses the keyword None, as shown as Listing 1-6, to indicate that it’s    Instead, Python uses the keyword None, as shown in Listing 1-6, to indicate that it’s   Pending
       different from any numerical value for zero, an empty list, or an empty string.          different from any numerical value for zero, an empty list, or an empty string.

7                                                                                                                                                                                       Print 2
        # 2. Insert                                                                              # 2. Insert
        l = [1, 2, 4]                                                                            l = [1, 2, 4]
        l.insert(2, 3)                                                                           l/insert(2, 2)
        print(l)                                                                                 print(l)
        # [1, 2, 3, 4]                                                                           # [1, 2, 2, 4]


7      You can easily remove an element x from a list by using the list method remove(x):       You can easily remove the first occurrence of element x from a list by using the list   Print 2
                                                                                                method remove(x):

8      The method index(x) finds the first occurrence of the element x in the list and          The list.index(x) method returns the index of the element x in the list. You can        Print 2
       returns its index.                                                                       use optional start and stop arguments to limit the index range to search.

8      The stack data structure works intuitively as a first-in, first-out (FIFO) structure.    The stack data structure works intuitively as a last-in, first-out (LIFO) structure.    Pending

35     Insertion                                                                                You need to install the matplotlib library with pip install matplotlib in your          Print 2
                                                                                                terminal or shell to use this example in your own programs.

40                                                                                                                                                                                      Print 2
        mark = [(True, s) if 'anonymous' in s else (False, s) for s in txt]                      mark = [('anonymous' in s, s) for s in txt]


65     Insertion                                                                                Roughly speaking, the axis parameter controls which axis will be aggregated or          Print 2
                                                                                                collapsed when used in a NumPy operation such as np.sort().

74     For the Daily Active Users column, every observed value that is smaller than 811.2 –     For the Daily Active Users column, every observed value that is smaller than 811.2 –    Pending
       152.97 = 658.23 or larger than 811.2 + 152.23 = 963.43 is considered an outlier.         152.97 = 658.23 or larger than 811.2 + 152.97 = 964.17 is considered an outlier.

92                                                                                                                                                                                      Print 2
        ## One-liner                                                                             ## One-liner
        model = LogisticRegression().fit(X[:,0].reshape(n,1), X[:,1])                            model = LogisticRegression().fit(X[:,0].reshape(-1,1), X[:,1])


129    This example uses the findall() method of the re module.                                 This example uses the findall() function of the re module.                              Pending
                                                                                                                                                                                       Print
Page                                           Error                                                                               Correction
                                                                                                                                                                                     corrected

141    The regular expression expects a positive number of (lowercase) characters, numbers,   The regular expression expects a positive number of (lowercase) characters, numbers,   Print 3
       underscores, hyphens, or dots ([a-z0-9_\-\.]+). Note that you need to escape the       underscores, hyphens, or dots ([a-z0-9_\-.]+). Note that you need to escape the
       hyphen (\-) because it normally indicates a range within the square brackets.          hyphen (\-) because it normally indicates a range within the square brackets. Note
       Similarly, you need to escape the dot (\.) because you actually want to match          that the dot (.) doesn’t need to be escaped here, because within a character
       the dot and not an arbitrary character.                                                class [], it doesn’t match an arbitrary character but the actual point character
                                                                                              '.'.

168    A prime number n is an integer that’s not divisible without a remainder by any other   A prime number n is an integer that’s not divisible without a remainder by any other   Print 2
       integer, except for i and n.                                                           integer, except for 1 and n.
