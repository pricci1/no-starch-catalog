                                                                     Think Like a Programmer
                                                             An Introduction to Creative Problem Solving
                                                                                         by V. Anton Spraul

                                                                                    Errata updated to print 16
                                                                                                                                                                                                  Print
Page                                             Error                                                                                     Correction
                                                                                                                                                                                                corrected

32     As before, we double every second digit, starting with the digit to the right of the         As before, we double every second digit, starting with the digit to the left of the check   Print 2
       check digit, and add the values of all digits, including the check digit, to determine the   digit, and add the values of all digits, including the check digit, to determine the
       checksum.                                                                                    checksum.

34                                                                                                                                                                                              Print 2
           if (doubledDigit > 10) sum = 1 + doubledDigit % 10;                                          if (doubledDigit >= 10) sum = 1 + doubledDigit % 10;


38                                                                                                                                                                                              Print 2
           else checksum += 2 * (digit - '0');                                                          else checksum += doubledDigitValue(digit - '0');


41     So an input value of 56 in lowercase mode would yield the letter b because 57 modulo         So an input value of 56 in lowercase mode would yield the letter b because 56 modulo        Print 2
       27 is 2 and b is the second letter in the alphabet.                                          27 is 2 and b is the second letter in the alphabet.

46,                                                                                                                                                                                             Print 3
47,     Numbered entered                                                                             Number entered
48

53     2.1 Using the same rule as the shapes programs from earlier in the chapter                   2.1 Using only single-character output statements that output a hash mark, a                Print 2
       (only two output statements—one that outputs the hash mark and one that                      space, or an end-of-line, write a program that produces the following shape:
       outputs an end-of-line), write a program that produces the following shape:

60     If intA > intB, for example, we want to return a positive number, and intA – intB will       If intA* > intB*, for example, we want to return a positive number, and intA* – intB*       Print 3
       be positive if intA > intB. Likewise, intA – intB will be negative if intB > intA and will   will be positive if intA* > intB*. Likewise, intA* – intB* will be negative if intB* >
       be zero when the two integers are equal.                                                     intA* and will be zero when the two integers are equal.

66                                   1 and not the value in location[0].
       Therefore, we initialize it to 0                                                                                           1 and not the value in location [0].
                                                                                                    Therefore, we initialize it to 0                                                            Print 2

71     Here, the variable highPosition 1 takes the place of highest. Because we aren’t              Here, the variable highPosition 1 takes the place of highest. Because we aren’t             Print 3
       directly tracking the grade closest to the average, when it’s time to compare the            directly tracking the highest grade, when it’s time to compare the highest grade
       closest grade against the current grade, we use highPosition as a reference into                                                                                               2
                                                                                                    against the current grade, we use highPosition as a reference into studentArray . If
       studentArray  2  . If the grade in the current array position is higher, the current         the grade in the current array position is higher, the current position in our processing
                                                                     3
       position in our processing loop is assigned to highPosition . Once the loop is over,                                            3
                                                                                                    loop is assigned to highPosition . Once the loop is over, we can access the name of
       we can access the name of the student with the grade closest to the average using            the student with the highest grade using studentArray[highPosition].name, and we
       studentArray[highPosition].name, and we can also access any other data related to that       can also access any other data related to that student record.
       student record.
                                                                                                                                                                                               Print
Page                                             Error                                                                                Correction
                                                                                                                                                                                             corrected

72     In a double loop that processes a two-dimensional array, however, the meaningful         In a double loop that processes a two-dimensional array, however, the meaningful             Print 2
       identifiers help me keep my dimensions and subscripts straight because I can look up     identifiers help me keep my dimensions and subscripts straight because I can look up
       and see that I am using agent in the same dimension where I used numAgents in the        and see that I am using agent in the same dimension where I used NUM_AGENTS in the
       array declaration.                                                                       array declaration.

72                                                                                                                                                                                           Print 2
            double average = (sum + 0.5) / ARRAY_SIZE;                                               double average = sum / ARRAY_SIZE;


73                                                                                                                                                                                           Print 2
        int highestAverage =   1
                               arrayAverage(sales[0], 12);                                       double highestAverage =   1
                                                                                                                           arrayAverage(sales[0], 12);
        for (int agent = 1; agent < NUM_AGENTS; agent++) {                                       for (int agent = 1; agent < NUM_AGENTS; agent++) {
            int agentAverage =     2
                                 arrayAverage(sales[agent], 12);                                     double agentAverage =     2
                                                                                                                             arrayAverage(sales[agent], 12);


77     Furthermore, reading all of the survey responses into the vector required a loop on      Furthermore, reading all of the grades into the vector required a loop on its own,           Print 2
       its own, separate from the loops to process all of the survey responses and find the     separate from the loops to process all of the survey responses and find the highest
       highest value in the histogram.                                                          value in the histogram.

89–                                                                         3
       All the function does is increment count and make a recursive call . Recursion is                                                  2                            3
                                                                                                All the function does is increment count and make a recursive call . Recursion is            Print 2
90     discussed extensively in Chapter 6 but is used here simply to make the chain of          discussed extensively in Chapter 6 but is used here simply to make the chain of
       function calls as long as possible. The activation record of a function remains on the   function calls as long as possible. The activation record of a function remains on the
       stack until that function ends. So when the first call is made to stackOverflow from     stack until that function ends. So when the first call is made to stackOverflow from
       main, an activation record is placed on the runtime stack that cannot be removed until       4
                                                                                                main , an activation record is placed on the runtime stack that cannot be removed
       that first function call ends.                                                           until that first function call ends.

94     Including the allocated memory in our diagrams helps remind us to actually perform       Including the deallocated memory in our diagrams helps remind us to actually                 Print 2
       the deallocation.                                                                        perform the deallocation.

101    addRecordThis function takes a pointer to a collection of student records (a student     addRecord This function takes a pointer to a collection of student records, a student        Print 2
       number and a grade), and it adds a new record with this data to the collection.          number, and a grade, and it adds a new record with this data to the collection.

102    Again, studentCollection is synonymous with node *, but for readability I’m using the    Again, studentCollection is synonymous with listNode *, but for readability I’m using        Print 2
       studentCollection type only for variables that will refer to the whole list structure.   the studentCollection type only for variables that will refer to the whole list structure.

124    (You might wonder how, if we’re validating grades as they are assigned, we could ever    (You might wonder how, if we’re validating grades as they are assigned, we could ever        Print 2
       have an invalid grade, but remember that our default constructor assigns –1 to           have an invalid grade, but we might decide to assign an invalid grade in the
       signal that no legitimate grade has been assigned yet.)                                  constructor to signal that no legitimate grade has been assigned yet.)

127    Unlike our node type in Chapter 4, this node doesn’t have individual fields for the      Unlike our node type in Chapter 4, this node doesn’t have individual fields for the          Print 2
       payload data but rather includes a member of the studentRecord struct type .    2        payload data but rather includes a member of the studentRecord class .  2
128    Now we can turn our attention to the last of the three member functions,                 Now we can turn our attention to the second member function in the list,                     Print 2
       recordWithNumber.                                                                        recordWithNumber.

140    In other words, if we didn’t have a class for our linked-list collection of student      In other words, if we didn’t have a class for our linked-list collection of student          Print 2
       records, we’re still responsible for deleting the nodes in the list when we we’re        records, we’re still responsible for deleting the nodes in the list when we’re through
       through with them.                                                                       with them.
                                                                                                                                                                         Print
Page                                         Error                                                                       Correction
                                                                                                                                                                       corrected

156                                                                                                                                                                    Print 6
       int arraySumRecursiveExtraParams(int integers[],                                int arraySumRecursiveExtraParameters(int integers[],
                                        int size,    1
                                                    int sum,   2 int currentIndex) {                                    int size,  1int sum,   2 int currentIndex) {
157                                                                                                                                                                    Print 2
       int zeroCountIterative(int numbers[], int size) {                               int zeroCountIterative(int numbers[], int size) {
          int sum = 0;                                                                    int count = 0;
          int count = 0;


158                                                                                                                                                                    Print 6
       struct listNnode {                                                              struct listNode {


163                                                                                                                                                                    Print 2
       class binaryTree {                                                              class binaryTree {
           public:                                                                         public:
            1  int countLeaves();                                                           1  int leafCount();
           private:                                                                        private:
               struct binaryTreeNode {                                                         struct binaryTreeNode {
                   int data;                                                                       int data;
                   binaryTreeNode * left;                                                          binaryTreeNode * left;
                   binaryTreeNode * right;                                                         binaryTreeNode * right;
               };                                                                              };
               typedef treeNode * treePtr;                                                     typedef binaryTreeNode * treePtr;
               treePtr _root;                                                                  treePtr _root;
       };                                                                              };


163                                                                                                                                                                    Print 2
       int numLeaves = bt.countLeaves();                                               int numLeaves = bt.leafCount();


164                                                                                                                                                                    Print 2
       int leafCount(treePtr rootPtr) {                                                int countLeaves(treePtr rootPtr) {
           if (rootPtr == NULL) return 0;                                                  if (rootPtr == NULL) return 0;
           if (rootPtr->right == NULL && rootPtr->left == NULL)                            if (rootPtr->right == NULL && rootPtr->left == NULL)
               return 1;                                                                       return 1;
           int leftCount = leafCount(rootPtr->left);                                       int leftCount = countLeaves(rootPtr->left);
           int rightCount = leafCount(rootPtr->right);                                     int rightCount = countLeaves(rootPtr->right);
           return leftCount + rightCount;                                                  return leftCount + rightCount;
       }                                                                               }
                                                                                                                                                                                                Print
Page                                            Error                                                                                    Correction
                                                                                                                                                                                              corrected

163                                                                                                                                                                                           Print 2
        int numLeaves = bt.countLeaves();                                                           int numLeaves = bt.leafCount();


165    Because the public function has access to the private data member root, it can pass this    Because the public function has access to the private data member _root, it can pass       Print 2
       along to the recursive function and then return the results back to the client like this:   this along to the recursive function and then return the results back to the client like
                                                                                                   this:
              class binaryTree {
                  public:                                                                                 class binaryTree {
                      int publicCountLeaves();                                                                public:
                  private:                                                                                        int publicLeafCount();
                      struct binaryTreeNode {                                                                 private:
                          int data;                                                                               struct binaryTreeNode {
                          binaryTreeNode * left;                                                                      int data;
                          binaryTreeNode * right;                                                                     binaryTreeNode * left;
                      };                                                                                              binaryTreeNode * right;
                      typedef binaryTreeNode * treePtr;                                                           };
                      treePtr _root;                                                                              typedef binaryTreeNode * treePtr;
                      int privateCountLeaves(treePtr rootPtr);                                                    treePtr _root;
              };                                                                                                  int privateLeafCount(treePtr rootPtr);
         1   int binaryTree::privateCountLeaves(treePtr rootPtr) {                                        };
                  if (rootPtr == NULL) return 0;                                                     1   int binaryTree::privateLeafCount(treePtr rootPtr) {
                  if (rootPtr->right == NULL && rootPtr->left == NULL)                                        if (rootPtr == NULL) return 0;
                      return 1;                                                                               if (rootPtr->right == NULL && rootPtr->left == NULL)
                  int leftCount = privateCountLeaves(rootPtr->left);                                              return 1;
                  int rightCount = privateCountLeaves(rootPtr->right);                                        int leftCount = privateLeafCount(rootPtr->left);
                  return leftCount + rightCount;                                                              int rightCount = privateLeafCount(rootPtr->right);
              }                                                                                               return leftCount + rightCount;
         2   int binaryTree::publicCountLeaves() {                                                        }
               3 return privateCountLeaves(_root);                                                   2   int binaryTree::publicLeafCount() {
              }                                                                                            3 return privateLeafCount(_root);
                                                                                                          }
       Although C++ would allow both functions to have the same name, for clarity I’ve used
       different names to distinguish between the public and private “count leaves”                Although C++ would allow both functions to have the same name, for clarity I’ve used
                                                   1
       functions. The code in privateCountLeaves is exactly the same as our previous,              different names to distinguish between the public and private “count leaves”
       independent function countLeaves. The wrapper function publicCountLeaves is    2                                                      1
                                                                                                   functions. The code in privateLeafCount is exactly the same as our previous,
       simple. It calls privateCountLeaves, passing the private data member root, and returns                                                                                 2
                                                                                                   independent function leafCount. The wrapper function publicLeafCount is simple.
       the result .3                                                                               It calls privateLeafCount, passing the private data member _root, and returns the result
                                                                                                   3  .

166                                                                                                                                                                                           Print 2
       1 stack<2 binaryTreeNode *> nodes;                                                          1 stack<2 treePtr> nodes;
167    Second, look how many function calls stackBasedCountLeaves makes—for each visit to          Second, look how many function calls stackBasedCountLeaves makes—for each visit to         Print 2
       an interior node (i.e., not a leaf), this function makes four function calls: one each to   an interior node (i.e., not a leaf), this function makes up to five function calls: one
       empty and top, and two to push.                                                             each to empty, top, and pop, and one or two to push.
                                                                                                                                                                                             Print
Page                                            Error                                                                                  Correction
                                                                                                                                                                                           corrected

178    The first two functions are very simple: higherGrade returns true when the first record   The first two functions are very simple: higherGrade returns true when the first record   Print 2
       has the higher grade, and lowerStudent number returns true when the first record has      has the higher grade, and lowerStudentNumber returns true when the first record has
       the lower student number.                                                                 the lower student number.

183    If integerList is a list<int>, populated with integers, and iter is a                     If intList is a list<int>, populated with integers, and iter is a list<int>::iterator,    Print 2
       list<int>::iterator, then we could display all of the integers in the list with the       then we could display all of the integers in the list with the following:
       following:

190    If sra is an array containing arraysize objects of type studentRecord, the resulting      If sra is an array containing arraySize objects of type studentRecord, the resulting      Print 2
       code looks like this:                                                                     code looks like this:

192                                                                                                                                                                                        Print 2
       1studentRecord sortArray[arraySize];                                                      1studentRecord * sortArray = new studentRecord[arraySize];
213                                                                                                                                                                                        Print 2
        void removeWordsWithoutLetter(list & wordList,                                            void removeWordsWithoutLetter(list & wordList,
                                       char requiredLetter) {                                                                    char requiredLetter) {
           list::const_iterator iter;                                                                list::iterator iter;


214                                                                                                                                                                                        Print 2
        void removeWordsWithLetter(list & wordList, char forbiddenLetter) {                       void removeWordsWithLetter(list & wordList, char forbiddenLetter) {
           list::const_iterator iter;                                                                list::iterator iter;


221                                                                                                                                                                                        Print 6
        integerListClass numberList = new integerListClass;                                       integerListClass numberList = new integerListClass();
        numberList.addInteger(15);                                                                numberList.addInteger(15);
