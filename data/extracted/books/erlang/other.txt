                                                Learn You Some Erlang for Great Good!
                                                                                   A Beginner’s Guide
                                                                                            by Fred Hebert

                                                                                    errata updated to print 8

                                                                                                                                                                                               Print
Page                                            Error                                                                                   Correction
                                                                                                                                                                                             corrected

38     Note that the .beam �le generated will no longer be portable across platforms.            Note that the .beam �le generated will contain both native and non-native code,             Print 3
                                                                                                 and the native part will not be portable across platforms.

62     The factorial of a number n is the product of the sequence 1 × 2 × 3 × ... × n, or        The factorial of a number n is the product of the sequence 1 × 2 × 3 × ... × n, or          Print 3
       alternatively n × n – 1 × n – 2 × ... × 1.                                                alternatively n × (n – 1) × (n – 2) × ... × 1.

128    In naive mode, the functions are gb_trees:enter/2, gb_trees:lookup/2, and                 In naive mode, the functions are gb_trees:enter/3, gb_trees:lookup/2, and                   Print 3
       gb_trees:delete_any/2.                                                                    gb_trees:delete_any/2.

138    URL replacement                                                                           Some studies proved that the main sources of downtime in large-scale software               Print 3
                                                                                                 systems are intermittent or transient bugs (see https://dslab.ep�.ch/pubs/crashonly.pdf).

153       1. A message to store food is sent from you (the shell) to the fridge process.            1. A message to take food is sent from you (the shell) to the fridge process.            Print 3
          ...                                                                                       ...
          3. The fridge stores the item and sends ok to your process.                               3. The fridge removes the item and sends it to your process.

154       1. A message to store food is sent from you (the shell) to an unknown process.            1. A message to take food is sent from you (the shell) to an unknown process.            Print 3

188                                                                                                                                                                                          Print 3
        error:function_clause -> %% not in {{D,M,Y},{H,Min,S}} format                             error:function_clause -> %% not in {{Y,M,D},{H,Min,S}} format
          false                                                                                     false



224    The sitting/2 function can then return the tuples {next_state, NextStateName,             The sitting/2 function can then return the tuples {next_state, NextStateName,               Print 3
       NewStateData}, {next_state, NextStateName, NewStateData, Timeout}, {next_state,           NewStateData}, {next_state, NextStateName, NewStateData, Timeout}, {next_state,
       NextStateName, hibernate}, and {stop, Reason, NewStateData}.                              NextStateName, NextStateData, hibernate}, and {stop, Reason, NewStateData}.

269    StartFunc is a tuple that speci�es how to start the supervisor.                           StartFunc is a tuple that speci�es how to start the child.                                  Print 3

269    So instead of doing supervisor:start_child(Sup, Spec), which would call                   So instead of doing supervisor:start_child(Sup, Spec), which would call                     Print 3
       erlang:apply(M,F,A), we now have supervisor:start_child(Sup, Args), which calls           erlang:apply(M,F,A), we now have supervisor:start_child(Sup, Args), which calls
       erlang:apply(M,F,Args++A).                                                                erlang:apply(M,F,A++Args).
                                                                                                                                                                                               Print
Page                                            Error                                                                                   Correction
                                                                                                                                                                                             corrected

291                                                                                                                                                                                          Print 3
        -define(SPEC(MFA),                                                                         -define(SPEC(MFA),
            {worker_sup,                                                                               {worker_sup,
            {ppool_worker_sup, start_link, [MFA]},                                                     {ppool_worker_sup, start_link, [MFA]},
            permanent,                                                                                 temporary,



291                                                                                                                                                                                          Print 3
        init({Limit, MFA, Sup}) ->                                                                 init({Limit, MFA, Sup}) ->
          {ok, Pid} = supervisor:start_child(Sup, ?SPEC(MFA)),                                       {ok, Pid} = supervisor:start_child(Sup, ?SPEC(MFA)),
          {ok, #state{limit=Limit, refs=gb_sets:empty()}}.                                           link(Pid),
                                                                                                     {ok, #state{limit=Limit, refs=gb_sets:empty()}}.



292                                                                                                                                                                                          Print 3
        handle_info({start_worker_supervisor, Sup, MFA}, S = #state{}) ->                          handle_info({start_worker_supervisor, Sup, MFA}, S = #state{}) ->
          {ok, Pid} = supervisor:start_child(Sup, ?SPEC(MFA)),                                       {ok, Pid} = supervisor:start_child(Sup, ?SPEC(MFA)),
          {noreply, S#state{sup=Pid}};                                                               link(Pid),
                                                                                                     {noreply, S#state{sup=Pid}};



307    This tells OTP that when starting your application, it should call                         This tells OTP that when starting your application, it should call                         Print 3
       CallbackMod:start(normal, Args). It will also call CallbackMod:stop(Args) when             CallbackMod:start(normal, Args). This function's return value will be used when
       stopping it.                                                                               OTP will call CallbackMod:stop(StartReturn) when stopping your application.

341    If you’re using pure Erlang code without native compiling with HiPE (a native              If you’re using pure Erlang code, then that code will be portable.                         Print 5
       compiler for Erlang code, which gives somewhat faster code, especially for
       CPU-bound applications), then that code will be portable.

351,                                                                                                                                                                                         Print 3
367     {app, stdlib, [{mod_cond, derived}, {incl_cond, include}]}                                 {app, stdlib, [{incl_cond, include}]}



383    Note that closing an accept socket will close that socket alone, and closing a listen      Note that closing an accept socket will close that socket alone, and closing a listen      Print 3
       socket will close all of the related accept sockets.                                       socket will close none of the related and established accept sockets, but will
                                                                                                  interrupt currently running calls to accept new ones.

395                                                                                                                                                                                          Print 3
        handle_info({tcp_closed, _Socket, _}, S) ->                                                handle_info({tcp_closed, _Socket}, S) ->



402    It’s called that because, secretly, the underlying implementation of ?_assert(A == B) is   It’s called that because, secretly, the underlying implementation of ?_assert(A == B) is   Print 3
       fun() -> ?assert(A,B) end; that is to say, it’s a function that generates a test.          fun() -> ?assert(A==B) end; that is to say, it’s a function that generates a test.
                                                                                                                                                                                           Print
Page                                             Error                                                                                  Correction
                                                                                                                                                                                         corrected

406                                                                                                                                                                                      Print 3
        some_test2_() ->                                                                          some2_test_() ->
          {foreach                                                                                  {foreach



407                                                                                                                                                                                      Print 3
        some2_tricky_test_()                                                                      some_tricky_test2_()



435                                                                                                                                                                                      Print 3
        ?MODULE = ets:new(regis, [set, named_table, protected])                                   ?MODULE = ets:new(?MODULE, [set, named_table, protected])



439    Note that we use regis (?MODULE) as the table name here . . .                             Note that we use regis_server (?MODULE) as the table name here . . .                    Print 3

459,   net_kernel:connect                                                                        net_kernel:connect_node                                                                 Print 5
463,
465,
534,
536,
538

465    Aha! The node didn’t connect to ketchup . . .                                             Aha! The node didn’t connect to salad . . .                                             Print 3

466    The ketchup node will never see any connection to olives . . .                            The salad node will never see any connection to olives . . .                            Print 3

466    . . . set a range of 15 ports to be used for Erlang nodes.                                . . . set a range of 16 ports to be used for Erlang nodes.                              Print 3

467    By default, the heartbeat delay (also called tick time) is set to 15 seconds, or 15,000   By default, the heartbeat delay is set to 15 seconds, or 15,000 milliseconds. After 4   Print 3
       milliseconds.                                                                             failed heartbeats, a remote node is considered dead. The heartbeat delay
                                                                                                 multiplied by 4 is called the tick time.

506                                                                                                                                                                                      Print 3
        {logdir, [all_nodes, master], "./logs/"}.                                                 {logdir, all_nodes, "./logs/"}.
                                                                                                  {logdir, master, "./logs/"}.



506    To truly include all nodes, [all_nodes, master] is required.                              To truly include all nodes, both all_nodes and master are required.                     Print 3

545                                                                                                                                                                                      Print 3
        foo(X) when is_integer(X) -> X + 1.                                                       foo(X) when is_integer(X) -> X + 1;
