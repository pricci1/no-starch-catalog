                                                              Attacking Network Protocols
                                                A Hacker’s Guide to Capture, Analysis, and Exploitation
                                                                                      by James Forshaw

                                                                                  errata updated to print 7
                                                                                                                                                                                               Print
Page                                            Error                                                                                    Correction
                                                                                                                                                                                             corrected

3      This layer contains network protocols, such as the HyperText Transport Protocol            This layer contains network protocols, such as the HyperText Transport Protocol            Print 2
       (HTTP), which transfers web page contents; the Simple Mail Transport Protocol              (HTTP), which transfers web page contents; the Simple Mail Transport Protocol
       (SMTP), which transfers email; and the Domain Name System (DNS) protocol, which            (SMTP), which transfers email; and the Domain Name System (DNS) protocol, which
       converts a name to a node on the network.                                                  converts a name to an address of a node on the network.

6      Ethernet uses a 64-bit value called a Media Access Control                                 Ethernet uses a 48-bit value called a Media Access Control                                 Print 2

28–    The Windows system proxy supports only SOCKS version 4 proxies, which means it             The Windows system proxy supports only SOCKS version 4 proxies, which means it             Print 2
29     will resolve only local hostnames.                                                         will resolve hostnames locally.

42     Because the Internet RFCs invariably use big endian as the preferred type for all          Because the Internet RFCs invariably use big endian as the preferred type for all          Print 2
       network protocols they specify (unless there are legacy reasons for doing otherwise),      network protocols they specify (unless there are legacy reasons for doing otherwise),
       big endian is referred as network order.                                                   big endian is referred to as network order.

60     If percent encoding was used to encode the value in Figure 3-18, you would get             If percent encoding was used to encode the value in Figure 3-18, you would get             Print 2
       %06%E3%58.                                                                                 %06%E3X.

137    If you look at the files distributed with a .NET application, you’ll see files with .exe   If you look at the files distributed with a .NET application, you’ll see files with .exe   Print 2
       and .ddl extensions, and you’d be forgiven for assuming they’re just native executables.   and .dll extensions, and you’d be forgiven for assuming they’re just native executables.

169    One advantage to this model is that because only public key information is ever            One advantage to this model is that because only public key information is ever            Print 2
       distributed, it’s possible to provide component certificates to users via public           distributed, it’s possible to provide root certificates to users via public networks.
       networks.

217    In the C language, this usually results in an error value being returned from the          In the C language, this usually results in an error value being returned from the          Print 2
       allocation functions (usually a NUL pointer); in other languages, it might result in the   allocation functions (usually a NULL pointer); in other languages, it might result in
       termination of the environment or the generation of an exception.                          the termination of the environment or the generation of an exception.
                                                                                                                                                                                      Print
Page                                           Error                                                                                Correction
                                                                                                                                                                                    corrected

224                                                                                                                                                                                 Print 2
        def bubble_sort(int[] buf)                                                              def bubble_sort(int[] buf)
        {                                                                                       {
          do                                                                                      do
          {                                                                                       {
            bool swapped = false;                                                                   bool swapped = false;
            int N = len(buf);                                                                       int N = len(buf);
            for(int i = 1; i < N - 1; ++i)                                                          for(int i = 1; i < N; ++i)
            {                                                                                       {
              if(buf[i-1] > buf[i])                                                                   if(buf[i-1] > buf[i])
              {                                                                                       {
                // Swap values                                                                          // Swap values
                swap( buf[i-1], buf[i] );                                                               swap( buf[i-1], buf[i] );
                swapped = true;                                                                         swapped = true;
              }                                                                                       }
            }                                                                                       }
        } while(swapped == false);                                                                } while(swapped);
        }                                                                                       }


253    As an example, consider the networked application shown in Listing 107.                 As an example, consider the networked application shown in Listing 10-7.             Print 2

264    The addresses of the two strings with the final NUL (which is represented by a 0) are   The addresses of the two strings with the final NULL (which is represented by a 0)   Print 2
       then pushed onto the stack in reverse order.                                            are then pushed onto the stack in reverse order.

264    Next, a single NUL is pushed on the stack for the environment array . . .               Next, a single NULL is pushed on the stack for the environment array . . .           Print 2

264                                                                                                                                                                                 Print 2
        ./test_shellcode execv.bin                                                              ./test_shellcode execve.bin
