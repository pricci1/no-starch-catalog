                                                                  Impractical Python Projects
                                                     Playful Programming Activities to Make You Smarter
                                                                                          by Lee Vaughan

                                                                                    errata updated to print 7

                                                                                                                                                                                                 Print
Page                                            Error                                                                                    Correction
                                                                                                                                                                                               corrected

33     dairy raid                                                                                  welsh slew                                                                                  Print 6

68                                                                                                                                                                                             Print 5
        for i in range(len(list_of_lists)):                                                         for nested_list in list_of_lists:
            print(list_of_lists[i])                                                                     print(nested_list)



79     Here’s the output of the program, using the ciphertext from Figure 4-2:                     Here’s the output of the program, using the ciphertext from Figure 4-3:                     Print 2

85                                                                                                                                                                                             Print 3
            row1 = (message[:row_1_len])                                                                row1 = (message[:row_1_len]).lower()
            row2 = (message[row_1_len:])                                                                row2 = (message[row_1_len:]).lower()



85                                                                                                                                                                                             Print 3
                plaintext.append(r1.lower())                                                                 plaintext.append(r1)
                plaintext.append(r2.lower())                                                                 plaintext.append(r2)



100                                                                                                                                                                                            Print 3
        Panel at east end of chapel slides                                                          Panelateastendofchapelslides



103    The cold tea didn’t please the old �nicky woman                                             So, the cold tea didn’t please the old �nicky woman                                         Print 3

111    A paragraph object has a variety of properties that specify its placement within a          A paragraph object has a variety of attributes that specify its placement within a          Print 5
       container—typically a page—and the way it divides its contents into separate lines.         container—typically a page—and the way it divides its contents into separate lines.
       You can access the formatting properties of a paragraph with the ParagraphFormat            You can access the formatting attributes of a paragraph with the ParagraphFormat
       object available through the ParagraphFormat property of the paragraph, and you can         object available through the ParagraphFormat attribute of the paragraph, and you can
       set all the paragraph properties using a paragraph style grouping or apply them directly    set all the paragraph attributes using a paragraph style grouping or apply them directly
       to a paragraph.                                                                             to a paragraph.
       A run is an inline-level object that occurs within paragraphs or other block-level          A run is an inline-level object that occurs within paragraphs or other block-level
       objects. A run object has a read-only font property providing access to a font object.      objects. A run object has a read-only font attribute providing access to a font object.
       A font object provides properties for getting and setting the character formatting for      A font object provides attributes for getting and setting the character formatting for
       that run. You’ll need this feature for setting your hidden message’s text color to white.   that run. You’ll need this feature for setting your hidden message’s text color to white.
                                                                                                                                                                                     Print
Page                                            Error                                                                                 Correction
                                                                                                                                                                                   corrected

116    De�ne a function that formats the spacing between paragraphs using python-docx’s         De�ne a function that formats the spacing between paragraphs using python-docx’s   Print 5
       paragraph_format property ➊.                                                             paragraph_format attribute ➊.

141                                                                                                                                                                                Print 3
                # mutate                                                                                 # mutate
                lock_wheel = int(randrange(0, len(combo)))                                               lock_wheel = randrange(0, len(combo))
               ➐ next_try[lock_wheel] = randint(0, len(combo)-1)                                        ➐ next_try[lock_wheel] = randint(0, 9)



156    . . . and adding the key/value pair (at any location, since dictionaries are             . . . and adding the key/value pair at any location.                               Print 5
       unordered).

164    Because of the very short training corpus, the moon is the only word pair with           Because of the very short training corpus, the moon is the only word pair with     Print 4
       multiple keys.                                                                           multiple values.

171    This is a far better solution than manually �nding and commenting out print()            This is a far better solution than manually �nding and commenting out calls to     Print 6
       statements!                                                                              print()!

182    Cool stars enter the                                                                     A line �ap-�apping                                                                 Print 5
       Window this hot evening all                                                              Across the dark crimson sky
       Heaven and earth ache                                                                    On this winter pond

205    The transformation to generate points over a unit disc is: x = √r*cos                    The transformation to generate points evenly over a unit disc is: x = √r*cosθ      Print 3
       The equations yield (x, y) values between 0 and 1.                                       The equations yield (x, y) values between –1 and 1.

218                                                                                                                                                                                Print 2
        >>> from random import randint                                                           >>> from random import randint
        >>> trials = 100000                                                                      >>> trials = 100000
        >>> success = 0                                                                          >>> success = 0
        >>> for trial in range(trials):                                                          >>> for trial in range(trials):
            faces = set()                                                                                faces = set()
            for rolls in range(6):                                                                       for rolls in range(6):
               roll = randint(1, 6)                                                                          roll = randint(1, 6)
               faces.add(roll)                                                                               faces.add(roll)
            if len(faces) == 6:                                                                          if len(faces) == 6:
                success += 1                                                                                 success += 1
        >>> print("probability of success = {}".format(success/trials))                          >>> print("probability of success = {}".format(success/trials))



250                                                                                                                                                                                Print 2
       8 prompt = '{} [{}]: '.format(prompt, default)                                               8 prompt = '{} [{}]: '.format(prompt, default)
       9 response = input(prompt)                                                                   9 response = input(prompt)
       0 if not response and default:                                                               0 if not response and default:



252    Set the default to 'sbc_blend', since this is theoretically the most stable mix of the   Set the default to 'bonds', in order to see how this supposedly ‘safe’ choice      Print 3
       four choices.                                                                            performs.
                                                                                                                                                                                             Print
Page                                            Error                                                                                  Correction
                                                                                                                                                                                           corrected

259    . . . a 4 percent withdrawal rate (equal to $80,000 per year), a 30-year retirement,      . . . a 4 percent withdrawal rate (equal to $80,000 per year), a 29-30-31 retirement      Print 3
       and 50,000 cases.                                                                         range, and 50,000 cases.

261                                                                                                                                                                                        Print 3
                      else:                                                                                    else:
                          withdraw_infl_adj = withdraw_infl_adj_2                                                  withdraw_infl_adj = withdraw_infl_adj_2

                  investments -= withdraw_infl_adj                                                             investments -= withdraw_infl_adj
                      investments = int(investments * (1 + i))                                                 investments = int(investments * (1 + i))



305    You’ll use the same transform_rotate() method you used to turn the satellite              You’ll use the same transform.rotate() method you used to turn the satellite              Print 3

329    The shell utilities module, shutil, provides high-level functions for working with �les   The shell utilities module, shutil, provides high-level functions for working with �les   Print 6
       and folders, such as copying, moving, renaming, and deleting.                             and folders, such as copying, moving, and deleting.

356                                                                                                                                                                                        Print 3
              ➎ first_digits[sample[0]] += 1                                                            ➎ first_digits[sample[0]] += 1
           ➏ data_count = [v for (k, v) in sorted(first_digits.items())]                               # check for missing digits
                                                                                                       keys = [str(digit) for digit in range(1, 10)]
                                                                                                       for key in keys:
                                                                                                           if key not in first_digits:
                                                                                                               first_digits[key] = 0
                                                                                                     ➏ data_count = [v for (k, v) in sorted(first_digits.items())]



357    Deletion                                                                                  Like all Python dictionaries, first_digits is unordered.                                  Print 5

360    This will work with no arguments, but set its size property to 15 and turn off the        This will work with no arguments, but set its size attribute to 15 and turn off the       Print 5
       frame around the legend for an arguably more attractive result.                           frame around the legend for an arguably more attractive result.
                                                                                                                                         Print
Page                                         Error                                                         Correction
                                                                                                                                       corrected

368                                                                                                                                    Print 3
       """Remove single-letter words from list if not 'a' or 'i'."""   """Remove single-letter words from list if not 'a' or 'i'."""
       word_list = ['a', 'nurses', 'i', 'stack', 'b', 'cats', 'c']     word_list = ['a', 'nurses', 'i', 'stack', 'b', 'c', 'cat']
                                                                       word_list_clean = []
       permissible = ('a', 'i')
                                                                       permissible = ('a', 'i')
       for word in word_list:
           if len(word) == 1 and word not in permissible:              for word in word_list:
               word_list.remove(word)                                      if len(word) > 1:
                                                                               word_list_clean.append(word)
       print("{}".format(word_list_clean))                                 elif len(word) == 1 and word in permissible:
                                                                               word_list_clean.append(word)
                                                                           else:
                                                                               continue

                                                                       print("{}".format(word_list_clean))



369                                                                                                                                    Print 5
       print(*digrams, sep='\n')                                       print(*sorted(digrams), sep='\n')
       --snip--                                                        --snip--
       for k in mapped:                                                for k in sorted(mapped):
