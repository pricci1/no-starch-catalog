                                                                  System Programming in Linux
                                                                               A Hands-On Introduction

                                                                                  by Stewart N. Weiss

                                                                               Errata updated to print 1
                                                                                                                                                                                      Print
Page                                          Error                                                                              Correction
                                                                                                                                                                                    corrected

2      Author’s note: The original version of K&R's program did not have a return type for                                                                                          Pending
       main(). This change is for historical purposes.


        #include                                                                              #include
        void main()                                                                           main()   /* NOTE: Original main() had no return type. */
        {                                                                                     {


3,     Author’s note: void main() generates warnings.                                                                                                                               Pending
16,
27,     void main()                                                                           int main()
28,
64,
65,
317

58     Linux kernels from 2.6 and later use the sysenter instruction, and the GNU C          Linux kernels from 2.6 and later use both the syscall and sysenter instructions, and   Pending
       Library glibc 2.3.2 and later use sysenter.                                           the GNU C Library glibc 2.3.2 and later use both syscall and sysenter.

125                                                                                                                                                                                 Pending
            char       ch;                       /* For option handling               */          int       ch;                      /* For option handling                */
                                                                                                                                                                                           Print
Page                                              Error                                                                                 Correction
                                                                                                                                                                                         corrected

156    In Figure 4-1, FS3 is missing an arrow.                                                                                                                                           Pending




239                                                                                                                                                                                      Pending
             BOOL             found = FALSE;             /* Flag to indicate match found   */         BOOL            found = FALSE;             /* Flag to indicate match found    */
             char             ch;                                                                     int             ch;


282    For example, if fd is a valid file descriptor for the file /var/loglastlog, then         For example, if fd is a valid file descriptor for the file /var/log/lastlog, then        Pending

        statx(fd, "", AT_EMPTY_PATH, STATX_ALL, &statxbuf);                                      statx(fd, "", AT_EMPTY_PATH, STATX_ALL, &statxbuf);

       refers to the file /var/log/lastlog.                                                     refers to the file /var/log/lastlog.

286                                                                                                                                                                                      Pending
             int             i;                                                                       int             i;
             char            ch;                                                                      int             ch;



307                                                                                                                                                                                      Pending
        $ ./spl_statfs /media/guest/USB_stick/                                                   $ ./spl_statfs /media/guest/USB_stick/
          File: "/media/guest/USB_stick/"                                                          File: "/media/guest/USB_stick/"
            ID: 840     0 Namelen: 1530    Type: msdos                                               ID: 84000000000 Namelen: 1530    Type: msdos



330                                                                                                                                                                                      Pending
        double f(int (*funcp)(int, int), int*);                                                  double f(int (*funcp)(int, int), int);
                                                                                                                                                                                       Print
Page                                            Error                                                                               Correction
                                                                                                                                                                                     corrected

335    In Linux and most Unix systems, the hierarchy has no cycles if it has no symbolic      In Linux and most Unix systems, the hierarchy usually has no cycles if it has no       Pending
       links.                                                                                 symbolic links.

377    For each name that we want to prepend, we get the string length of the name            For each name that we want to prepend, we get the string length of the name            Pending

        len = strlen{dirname};                                                                 len = strlen(dirname);

       and subtract it from front so that front points to where this name will start in the   and subtract it from front so that front points to where this name will start in the
       buffer.                                                                                buffer.

378                                                                                                                                                                                  Pending
         while ( (direntp = readdir dir_ptr)) != NULL ) {                                       while ( (direntp = readdir(dir_ptr) ) != NULL ) {
                errno = 0;                                                                             errno = 0;
                get_dev_ino(direntp->d_name, &this_entry);                                             get_dev_ino(direntp->d_name, &this_entry);


391    SIGTRAP   Used to implement debuggers and tracing programs such as strace and          SIGTRAP Used to implement debuggers such as gdb and tracing programs such as           Pending
       ptrace.                                                                                strace and ptrace.

404                                                                                                                                                                                  Pending
            printf("\ncleaning up in progress...\ndone\n");                                        printf("\ncleaning up in progress...\ndone\n");       /* UNSAFE */
            raise(SIGTSTP);                                                                        raise(SIGTSTP);
            printf("raise() called to stop process.\n");                                           printf("raise() called to stop process.\n");          /* UNSAFE */


411    If we enter CTRL-C 6 times while it is blocked, when it becomes unblocked, the         If we enter CTRL-C 6 times while it is blocked, when it becomes unblocked, the         Pending
       handler will run just once, not 10 times.                                              handler will run just once, not 6 times.
                                                                                                                                                                            Print
Page                                         Error                                                                          Correction
                                                                                                                                                                          corrected

422                                                                                                                                                                       Pending
        #include <fenv.h>                                                                  #include <fenv.h>

        void fpe_handler(int signo, siginfo_t *info, void *context)                        BOOL noreturn = FALSE;
        { /* These calls to printf() are all UNSAFE. */                                    void fpe_handler(int signo, siginfo_t *info, void *context)
            printf("Signal: %s\n", strsignal(info->si_signo));                             { /* These calls to printf() are all UNSAFE. */
            switch ( info->si_code ) {                                                         printf("Signal: %s\n", strsignal(info->si_signo));
            case FPE_INTDIV:                                                                   switch ( info->si_code ) {
                printf("Code: FPE_INTDIV (Integer divide by zero)\n"); break;                  case FPE_INTDIV:
            case FPE_FLTDIV:                                                                       printf("Code: FPE_INTDIV (Integer divide by zero)\n"); break;
                printf("Code: FPE_FLTDIV (Floating-point divide by zero)\n"); break;           case FPE_FLTDIV:
            case FPE_FLTOVF:                                                                       printf("Code: FPE_FLTDIV (Floating-point divide by zero)\n"); break;
                printf("Code: FPE_FLTOVF (Floating-point overflow)\n"); break;                 case FPE_FLTOVF:
                --snip--                                                                           printf("Code: FPE_FLTOVF (Floating-point overflow)\n"); break;
            }                                                                                      --snip--
                                                                                               }
            raise(SIGTERM);
        }                                                                                      if ( !noreturn )
                                                                                                   raise(SIGTERM); /* To force program to quit */
                                                                                           }


442    The timespec_diff() function introduced on page 442 and used in several                                                                                            Pending
       programs will not produce a correct time difference when the first time             /** timespec_diff(ts1, ts2, *diff) computes the difference ts1 - ts2,
       argument represents an older time than the second one. It is intended to be         storing it in *diff provided that ts2 is no later than ts1. */
       used only when the first is either equal to or later than the second. This is how
       it is used in all programs that call it. The book’s source code repository has
       another version, timespec_diff2(), that handles this case correctly. The assert.h   void timespec_diff(struct timespec ts1, struct timespec ts2,
       header file should be included in time_utils.c where the function is defined.                          struct timespec *diff)
                                                                                           {
                                                                                              long temp;
                                                                                              diff->tv_sec = ts1.tv_sec - ts2.tv_sec;
                                                                                              temp = ts1.tv_nsec - ts2.tv_nsec;
                                                                                              assert ( diff->tv_sec >= 0 );
                                                                                           1  if ( temp < 0 ) {
                                                                                                  /* Because temp < 0, we need to borrow 1 sec from tv_sec and
                                                                                                     add it to tv_nsec as 1000000000 nanoseconds. */
                                                                                                  diff->tv_sec--;
                                                                                                  diff->tv_nsec = 1000000000 + temp;
                                                                                              }
                                                                                              else
                                                                                                  diff->tv_nsec = temp;
                                                                                           }
                                                                                                                                                                                     Print
Page                                            Error                                                                                Correction
                                                                                                                                                                                   corrected

461    Author’s note: There is an extremely tiny probability of a race condition due to the                                                                                        Pending
       call to draw_initial_bar() preceding the signal handler setup in Listing 9-6. It should    int main(int argc, char *argv[])
       be placed after the two calls to sigaction().                                              {
                                                                                                      struct sigaction      act;
                                                                                                      const struct timespec slight_pause = {2, 0}; /* 2-second interval */
                                                                                                      struct timespec       remaining_sleep;
                                                                                                      draw_initial_bar();
                                                                                                      sigemptyset(&(act.sa_mask));
                                                                                                      act.sa_flags = 0;
                                                                                                      act.sa_handler = sig_handler;
                                                                                                      if ( sigaction(SIGINT, &act, NULL) == -1 )
                                                                                                          fatal_error(errno, "sigaction");
                                                                                                      if ( sigaction(SIGQUIT, &act, NULL) == -1 )
                                                                                                          fatal_error(errno, "sigaction");
                                                                                                      draw_initial_bar();

                                                                                                      act.sa_handler = refresh_progressbar;


472                                                                                                                                                                                Pending
        bigfile                       0923331                                                     bigfile                      923331


486                                                                                                                                                                                Pending
            struct sigaction sa;                                                                      struct sigaction sa;
            char             c;                                                                       int              c;


509                                                                                                                                                                                Pending
        for ( i = 0; i < elf_header64->e_phnum; i++ ) {                                           for ( i = 0; i < elf_header64->e_phnum; i++ ) {
            print_progheader64(fd, &(phdr64[i]);                                                      print_progheader64(fd, &(phdr64[i]));
        }                                                                                         }



514                                                                                                                                                                                Pending
        #include <unistd.h>                                                                       #include <unistd.h>
        #include <unistd.h>



543    In modern Linux systems, the physical pages of the parent’s memory image aren't           In modern Linux systems, the physical pages of the parent’s memory image aren't   Pending
       actually copied until the child process attempts to modify them.                          actually copied until one of the processes attempts to modify them.
                                                                                                                                                                                                         Print
Page                                              Error                                                                                        Correction
                                                                                                                                                                                                       corrected

548                                                                                                                                                                                                    Pending
        int main(int argc, char *argv[])                                                                int main(int argc, char *argv[])
        {                                                                                               {
            pid_t newpid;                                                                                   pid_t newpid;
            int num_children = 4;                                                                           int num_children = 3;


581                                                                                                                                                                                                    Pending
        $ ./waitpid_demo                                                                                $ ./waitpid_demo
        Producer PID = 37837                                                                            Producer PID = 37837
        Producer is about to start producing data.                                                      Producer is about to start producing data.
        Consumer PID = 37838                                                                            Consumer PID = 37838
        Data from producer, converted to uppercase:                                                     Data from producer, converted to uppercase:
        PPPPP                                                                                           PPPPP
        Process 37837 stopped by signal 19   # Issued kill -19 37837 from 2nd terminal                  Process 37837 stopped by signal 19   # Issued kill -19 37837 from 2nd terminal
        PPPP                                                                                            PPPP
        Process 37837 continued              # Issued kill -18 37837 from 2nd terminal                  Process 37837 continued              # Issued kill -18 37837 from 2nd terminal
        Producer finished; waiting for consumer to finish.                                              Producer (PID=37837) finished producing data; exiting
        Process 37838 stopped by signal 19   # Issued kill -19 37838 from 2nd terminal
        Process 37838 continued              # Issued kill -18 37838 from 2nd terminal                  Process 37837 exited with status 63
        PPPPPPPPP
        Process 37838 terminated by signal 8 # Issued kill -8 37838 from 2nd terminal                   Producer finished; waiting for consumer to finish.
        $                                                                                               Process 37838 stopped by signal 19   # Issued kill -19 37838 from 2nd terminal
                                                                                                        Process 37838 continued              # Issued kill -18 37838 from 2nd terminal
                                                                                                        PPPPPPPPP
                                                                                                        Process 37838 terminated by signal 8 # Issued kill -8 37838 from 2nd terminal
                                                                                                        $


582    An entirely different approach is to utilize the SIGCHLD signal. Normally, when a               An entirely different approach is to utilize the SIGCHLD signal. Normally, when a               Pending
       process terminates by calling an exit function or is killed or stopped by a signal, the         process terminates by calling an exit function or is killed or stopped by a signal, the
       kernel generates a SIGCHlD signal and sends it to that process’s parent, provided that it’s     kernel generates a SIGCHLD signal and sends it to that process’s parent, provided that it’s
       still running. There are exceptions to this rule that I’ll explain shortly, but for now let’s   still running. There are exceptions to this rule that I’ll explain shortly, but for now let’s
       assume that the SIGCHlD signal is always sent to the parent process in these                    assume that the SIGCHLD signal is always sent to the parent process in these
       circumstances. In this alternative method, we put the call to wait() or waitpid() inside        circumstances. In this alternative method, we put the call to wait() or waitpid() inside
       the SIGCHlD handler.                                                                            the SIGCHLD handler.

614                                                                                                                                                                                                    Pending
        t4: P1 executes mov @counter, register1                {counter     == 5 }                      t4: P1 executes mov @counter, register1                {counter     == 6 }


623                                                                                                                                                                                                    Pending
        data = sharedbuf->buf[sharedbuf->rear];                                                         data = sharedbuf->buf[sharedbuf->rear];
        sharedbuf->rear = (sharedbuf->rear + 1) % BUF_SIZE;                                             sharedbuf->front = (sharedbuf->front + 1) % BUF_SIZE;



635    1. Configure a sigevent structure sigev as follows:                                             1. Configure a sigevent structure sev as follows:                                               Pending
                                                                                                                                                                                                     Print
Page                                              Error                                                                                      Correction
                                                                                                                                                                                                   corrected

636                                                                                                                                                                                                Pending
        struct sigaction sa;                                                                            struct sigaction sigact;


639                                                                                                                                                                                                Pending
        unsigned short int rows, cols;         /* Size of terminal window          */                   unsigned short int rows, cols;        /* Size of terminal window        */
                                                                                                        msgtype         newmsg;               /* Message struct for message     */


658    If such a function existed, we could copy the pipe’s write end descriptor into the              If such a function existed, we could copy the pipe’s write end descriptor into the          Pending
       descriptors for standard input and output.                                                      descriptor for standard output.

707                                                                                                                                                                                                Pending
             2. Modify calc_client.c so that if it has no command line argument, it reads the                2. Modify spl_calc_client.c so that if it has no command line argument, it reads
                expression from standard input.                                                                 the expression from standard input.
             3. Modify spl_calc.h, calc_client.c, and calc_server.c so that if the user supplies a -         3. Modify spl_calc.h, spl_calc_client.c, and spl_calc_server.c so that if the user
                l command line option to the client, the server will request bc to load its                     supplies a -l command line option to the client, the server will request bc to
                standard math library, as described on its man page.                                            load its standard math library, as described on its man page.
             4. Modify both calc_client.c and calc_server.c so that if the user supplies an -s               4. Modify both spl_calc_client.c and spl_calc_server.c so that if the user supplies
                value command line option, the number of digits to the right of the decimal                     an -s value command line option, the number of digits to the right of the
                point for all answers will be the supplied value.                                               decimal point for all answers will be the supplied value.
             5. Write a version of calc_server.c that still relies upon the bc command but does              5. Write a version of spl_calc_server.c that still relies upon the bc command but
                not call popen() to run it.                                                                     does not call popen() to run it.
             6. Add a -d option to calc_server.c that, when present, turns it into a daemon,                 6. Add a -d option to spl_calc_server.c that, when present, turns it into a
                and when not, requires the user to run it in the background.                                    daemon, and when not, requires the user to run it in the background.


722                                                                                                                                                                                                Pending
        thread_data[t].task_id = t;                                                                     thread_data[t].id = t;


785                                                                                                                                                                                                Pending
            BOOL                 reader_preference = FALSE;                                                 BOOL                reader_preference = FALSE;
            char                 ch;                                                                        int                 ch;
                                                                                                                                                                                  Print
Page                                        Error                                                                           Correction
                                                                                                                                                                                corrected

814                                                                                                                                                                             Pending
       while ( more_data_to_read ) {          /* While source file has more data */        while ( more_data_to_read ) {             /* While source file has more data */
           // if ( aio_read() completed ) {                                                    if ( aio_read() completed ) {
           num_read = aio_return(&aio_block); /* Get num bytes read.              */               num_read = aio_return(&aio_block); /* Get num bytes read.             */
           writebuf = aio_block.aio_buf;      /* write() from buffer just filled */                writebuf = aio_block.aio_buf;      /* write() from buffer just filled */
           i = i^1;                           /* Same as i = (i == 0) ? 1 : 0     */               i = i^1;                           /* Same as i = (i == 0) ? 1 : 0    */
           aio_block.aio_buf = buffer[i];     /* Use other buffer for next read. */                aio_block.aio_buf = buffer[i];     /* Use other buffer for next read. */
           aio_block.aio_offset += num_bytes_read;     /* Adjust offset for read. */               aio_block.aio_offset += num_bytes_read;     /* Adjust offset for read.*/
           aio_read(&aiocb));                          /* Request next read.      */               aio_read(&aiocb));                          /* Request next read.     */
           write(target_fd, writebuf, num_bytes_read); /* Start write().          */               write(target_fd, writebuf, num_bytes_read); /* Start write().         */
           }                                                                                   }
       }                                                                                   }


825                                                                                                                                                                             Pending
           for ( int i = 0; i < NSENDERS; i++ ) {                                              for ( int i = 0; i < NSENDERS; i++ ) {
               if ( pipe(pipefd[i]) == -1 ) /* Create pipe[pipefd[i]. */                           if ( pipe(pipefd[i]) == -1 ) /* Create pipe[pipefd[i]]. */


837                                                                                                                                                                             Pending




850                                                                                                                                                                             Pending
       Mask name              Description                                                  Mask name              Description


       NOFLSH                 Disable flushing the input and output queues when            NOFLSH                 Disable flushing the input and output queues when
                              generating signals for the INT, QUIT, and SUSP characters.                          generating signals for the INTR, QUIT, and SUSP characters.
                                                                                                                                                                                              Print
Page                                              Error                                                                                   Correction
                                                                                                                                                                                            corrected

851                                                                                                                                                                                         Pending
        $ bash --no-editing                                                                         $ bash --noediting


855                                                                                                                                                                                         Pending
                              Handling Excess Input in the Terminal                                                      Handling Excess Input in the Terminal
            When read() reads from a terminal, it doesn’t receive characters until a newline            In canonical mode, when read() reads from a terminal, it doesn’t receive
            is entered. Assuming that ttyfd refers to a terminal, if a program calls read(ttyfd,        characters until a newline is entered (or the user terminates input by entering
            &buffer, 32) but the user enters 40 characters and then a newline, read() will              the end-of-file character). Assuming that ttyfd refers to a terminal, if a
            receive the first 32 characters and store them into buffer, but there will be 8             program calls read(ttyfd, &buffer, 32) but the user enters 40 characters and
            characters and a newline remaining in the input queue. A second call to                     then a newline, read() will receive the first 32 characters and store them into
            read(ttyfd, &buffer, 32) will be satisfied immediately because there’s a newline            buffer, but there will be 8 characters and a newline remaining in the terminal’s
            in the input queue. The remaining 8 characters and the newline will be stored               input queue. A second call to read(ttyfd, &buffer, 32) will be satisfied
            into buffer. If a program doesn’t want these characters to be read into buffer, it          immediately because the input queue is not empty. The remaining 8 characters
            can call tcflush(ttyfd, TCIFLUSH) to remove them from the input queue before                and the newline will be stored into buffer. If a program doesn’t want these
            reading again.                                                                              characters to be read into buffer, it can call tcflush(ttyfd, TCIFLUSH) to remove
                                                                                                        them from the input queue before reading again.



874    fillqeueue.c                                                                                fillqueue.c                                                                              Pending

875    In the second terminal, turn off canonical mode and run fillqueue:                          In the second terminal, turn off canonical mode and run fillqueue (you may have to       Pending
                                                                                                   set cap_sys_admin on fillqueue as you did for fakeinput_demo):

933                                                                                                                                                                                         Pending
                   }                                                                                       }
               wrefresh(win);                                                                              wrefresh(win);
        }                                                                                           }


936                                                                                                                                                                                         Pending
        if ( *proclist != NULL )                                                                    if ( *proclist != NULL ) {




Additional Notes

Page 197
An alternative to the command

 cpp -v /dev/null -o /dev/null

is
 cpp -v /dev/null -P

which generates the same output.

Page 411
The code following the while loop in sigprocmask_demo1.c should never be reached. It is there to show how to use SIG_SETMASK to set the blocked signals to a previous signal set. It should have a
preceding comment noting that it’s unreachable as long as the while loop cannot be exited.

Page 423
Although it is not incorrect, the extra parentheses in this line

 sigemptyset(&(action.sa_mask));

are unnecessary, because the struct . operator has higher precedence than the address-of operator, &. It can also be:

 sigemptyset(&action.sa_mask);



Page 485
The posix_timer_demo1.c program relies on the assumption that the timer IDs returned by successive calls to timer_create() start at 0 and increase by 1. POSIX doesn’t require this. In the versions of
Linux on which I have tested this, they do behave this way. An actual application should not be based on this assumption.

Page 611
The two lines

 shmp->ptr1 = (char*)
 malloc(20);

are correct, but it is a single assignment that should be:

 shmp->ptr1 = (char*) malloc(20);



Page 612
Although it is not incorrect, the line

 printf("%s\n", (char*) (shmp->ptr2));

should be

 printf("%s\n", shmp->ptr2);

since shmp->ptr2 is a string pointer (char*).


Page 618
In unnamedsem_demo.c, although this assignment is correct:
 shmp = mmap(NULL,
     sizeof(sharedmem), PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);

for consistency, since I prefer explicitly casting the void pointers, it should be:

 shmp = (sharedmem*) mmap(NULL,
     sizeof(sharedmem), PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);



Page 633
Though it is not incorrect, the code after the while loop cannot be executed given the current form of the loop. The call to fatal_error() could be preceded by a call to mq_close() and the code after the
loop could be deleted.

Page 639
The macro definition in ulogger.c

 #define SIGUPDATE SIGRTMIN+1

is correct as is, but in general, it’s best to put parentheses around macro values that contain operators, i.e., (SIGRTMIN+1).

Page 642
The call to cleanup() at the end of ulogger.c is never reached and unnecessary.

Page 663
The POSIX specification says that writes are atomic if the number of bytes is less than or equal to PIPE_BUF.
   Writes are atomic as long as the number of bytes is smaller than PIPE_BUF.
should read:
   Writes are atomic if the number of bytes is smaller than PIPE_BUF.

Page 700
It is not an error, but it is inconsistent that I use O_NDELAY in this program instead of O_NONBLOCK. They are interchangeable:

 while ( ((rawtext_fd = open(msg.raw_text_fifo,
       O_WRONLY | O_NDELAY)) == -1) && (tries < MAXTRIES) ) {



Page 720
At the bottom of this page, in the short listing that suggests two ways to pass data to threads, the declarations int counter = 0; and int *on_heap = (int*) malloc(sizeof int); are file-scoped. This
code is really a straw man that leads to the better solution, and cannot be compiled because on_heap is assigned the return value of a function call.

The on_heap variable should be initialized in the main program before any threads are created:

 int *on_heap; /* Initialize in main() with on_heap = (int*) malloc(sizeof int); */



Page 731
Not all of the output of ps -L -eopid,ppid,tid,cmd | grep pthread_signal_demo is shown in the listing.
Page 793
The text should note that the program in Listing 17-1 doesn’t reset standard input to blocking mode just before it terminates because the shell does this automatically. This is an important observation—it
means that you cannot stop and restart this program or any program that enables non-blocking input unless you write explicit handlers for the SIGSTOP, SIGTSTP, and SIGCONT signals that reenable non-
blocking input when the program is resumed.

Page 835
Although the code is correct, the following changes make it more consistent:

     if ( -1 == write(STDOUT_FILENO, prompt, strlen(prompt)) )
         fatal_error(errno, "write");
     while ( read(STDIN_FILENO, &inbuf, 1) > 0 ) {
         inbuf = toupper(inbuf);
         if ( -1 == write(STDOUT_FILENO, &inbuf, 1) )
             fatal_error(errno, "write");
     }



Page 873
On Linux kernels since Linux 6.2, this program may not work unless you set the CAP_SYS_ADMIN capability on the binary. You’ll need superuser privilege. After building the executable, add the line

 $ sudo setcap cap_sys_admin=eip fakeinput_demo

before running:

 $ ./fakeinput_demo
 $ /usr/bin/echo 'This is a dangerous thing to do!'
 This is a dangerous thing to do!
 $
