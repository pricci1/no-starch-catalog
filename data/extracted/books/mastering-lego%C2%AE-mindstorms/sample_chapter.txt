                                                               introduction to
                                                                                                                          1
                                                          Python programming
One exciting thing about LEGO robotics is that you can program             and sounds to your robots and using the Hub buttons to con-
your robots by using Word Blocks, a visual programming                     trol what happens in your programs.


                                                                           first steps
language based on Scratch, or Python, a popular text-based
programming language. Word Blocks provides a great
introduction to coding, but Python takes your coding skills to
the next level, while still being very intuitive. Because Python           When you open a new Python project in the MINDSTORMS
code is written in English, you can generally get a sense of               App, you’ll see the coding area with some starting code already
what a program is doing just by reading it, even if you don’t              written for you. You’ll write your own code below that starting
understand all of the specific details.                                    code. The screen also has other useful features. To get you
     Knowing Python has benefits far beyond the world of                   ready to start programming in Python, you’ll examine two of
LEGO: the language is widely used in companies and univer-                 those features, the console and the Help Center. Then you’ll
sities. When you code in Python, you’re working with a                     take a closer look at the starting code.
programming language used by millions of professional coders
around the world.                                                          the console
     In this chapter, you’ll learn some of the basics of Python            At the bottom of the screen is an area called the console (see
programming in the MINDSTORMS App. You’ll try adding lights                Figure 1-1). This is where you’ll see error messages if your




                                                                                                                 Help Center




                                                                 Console

                           Figure 1-1: A Python project in the MINDSTORMS App
    code has any problems as it runs. You can also print messages          MSHub() creates an object representing your actual Hub. Other
    to the console from within your code, which can be a useful            objects might represent your motors or sensors.
    way to keep track of how your program is working.                           You create an object by giving it a name and telling your
         It’s a good idea to have the console open when you’re             program where to look for all the behaviors that go with it.
    programming with Python. To expand the console, click the              Those behaviors are called methods, and they’re defined in
    Console icon       at the bottom of the window.                        libraries like the ones imported at the start of the program.
                                                                           Writing hub = MSHub() is like saying, “Use the MSHub library to
    the Help Center                                                        create an object and name it hub.” The MSHub library includes
    The built-in Help Center is another valuable tool to help you          methods for controlling many parts of your robot—the buttons,
    program in Python. To open the Help Center, click the book             the speaker, the lights, the internal sensors, and the ports—
    icon     on the right side of the screen and choose the Python         everything that’s part of the Hub.
    option. The Help Center contains lots of useful information,                After you create an object, you can use its name inside your
    including detailed descriptions of the features available in           program anytime you want that object to do something—that is,
    MINDSTORMS Python.                                                     anytime you want to call one of the object’s methods. You call a
                                                                           method by adding a period after the object’s name, followed by
    the starting code
                                                                           the name of the method, followed by a set of parentheses. That’s
    As we already noted, when you create a new Python program in           exactly what happens in the last part of the starting code:
    the MINDSTORMS App, some code will already be in the coding
    area. Don’t delete any of this starting code—it does some impor-       # Write your program here.
    tant setup for your program. You don’t need to understand all          hub.speaker.beep()
    the starting code to begin programming with Python, but for the
    curious, here’s a rundown of what the code means. (This infor-                After another comment, the second line, hub.speaker.beep(),
    mation will be useful as you get further into writing Python code.)    calls the beep() method of the hub object’s speaker. This code is a
         The first few lines of the starting code are called imports:      bit like saying, “Hey Hub, use your speaker to play a beep.” If you
                                                                           write your program below this line, your program will start with a
    from mindstorms import MSHub, Motor, MotorPair, ↩
    ColorSensor, DistanceSensor, App                                       beep. If you don’t want the beep, delete this line of code. (However,
    from mindstorms.control import wait_for_seconds, wait ↩                don’t delete any of the lines above it.)
    _until, Timer
    from mindstorms.operator import greater_than, greater ↩
    _than_or_equal_to, less_than, less_than_or_equal_to, ↩
    equal_to, not_equal_to                                                     SPIKE PRIME STARTING CODE
    import math

         This code imports, or loads, the main libraries you’ll need for       New Python projects in the SPIKE Prime App also start
    programming your robot. Libraries are collections of code. These           with some code already in the coding area. As you can
    collections allow you to communicate with the various pieces of            see, it’s similar to the Robot Inventor starting code:
    MINDSTORMS hardware and to perform operations in programs.
                                                                               from spike import PrimeHub, LightMatrix, Button, ↩
    For example, to use the Color Sensor in your program, you must
                                                                               StatusLight, ForceSensor, MotionSensor, Speaker, ↩
    import the ColorSensor library. Once these libraries have been             ColorSensor, App, DistanceSensor, Motor, MotorPair
    imported, you can use them in the programs you write.                      from spike.control import wait_for_seconds, wait ↩
         Here’s the next part of the starting code:                            _until, Timer
                                                                               from math import *
    # Create your objects here.                                                hub = PrimeHub()
    hub = MSHub()                                                              hub.light_matrix.show_image('HAPPY')

         The first line, # Create your objects here, is a comment.                   The code begins by importing libraries. Then the
    In other words, it’s a note to yourself or anyone else looking at          line hub = PrimeHub() creates an object representing
    the program. Any line that starts with # is treated as a com-
                                                                               the Hub, much like in the Robot Inventor starting code.
    ment and isn’t executed when the program runs. It’s there just             The last line of code displays a smiley face on the Hub’s
    for your information. Including comments in your programs is a             light matrix. If you write your program below this line,
    good habit, since they make your code easier to understand.                your program will start with the Hub displaying a smi-
         The second line, hub = MSHub(), creates an object, a named            ley face, or you can delete this last line if you choose.
    piece of code that comes with a set of behaviors. Python objects
    often represent objects in the real world. In this case, hub =


2          CHAPTER 1
controlling Hub                                                         TRY THIS

outputs and inputs                                                      Write code to play the first line of “Mary Had a Little
                                                                        Lamb.” The notes are B, A, G, A, B, B, B.
                                                                            Here’s one possible solution:
The hub object comes with lots of methods for controlling the
Hub’s outputs and inputs. As you’ve already seen, the default           hub.speaker.beep(71, 0.5)
starting code calls a method that plays a beep. In fact, you can        hub.speaker.beep(69, 0.5)
write code to play many sounds, and Python also has methods             hub.speaker.beep(67, 0.5)
for controlling the Hub’s lights. These sounds and lights are           hub.speaker.beep(69, 0.5)
                                                                        hub.speaker.beep(71, 0.5)
examples of outputs—signals your program sends out into the
                                                                        hub.speaker.beep(71, 0.5)
world. You can also write programs that respond to the outside          hub.speaker.beep(71, 1)
world by taking inputs from the Hub’s buttons.

sounds
To make the Hub play a beep, as in the Robot Inventor starting
program, use this:                                                        In addition to playing beeps, you can play sound files,
                                                                    but to do this, you first have to create another object. This
hub.speaker.beep()                                                  is because most sound files play through your programming
                                                                    device’s speakers, not from the Hub. To access these sound
     Like many methods, beep() comes with a set of param-           files, first create an object by using the App library and give it
eters, options that let you customize the way the method will       the name app:
be executed. You set them inside the parentheses after the
method name. In the case of beep(), its parameters set the          app = App()
beep’s pitch and duration. Pitches are represented by num-
bers corresponding to MIDI notes, and durations are given in           Then use the app object’s play_sound() method, with the
seconds. For example, beep(67, 1.0) will play the note G (MIDI      name of the desired sound in the parentheses:
note number 67) for one second.
                                                                    app.play_sound('Emotional Piano')

 N O T E The system for numbering notes, such as 60 for
                                                                         The name of the sound must be given inside quotation
middle C, comes from MIDI. Short for Musical Instrument
                                                                    marks. Single or double quotes will work, as long as they
Digital Interface, MIDI is a standard way for electronic
                                                                    match—either both single or both double. You’ll find a list
devices to represent music.
                                                                    of the many available sounds in the App section of the
                                                                    Help Center.
     You must enter values for the parameters in the correct
                                                                         With play_sound(), the entire sound will play before the
order (pitch followed by duration), separated by a comma. If
                                                                    program moves on to the next line of code. You can also start
you don’t specify any values, the beep will play with the default
                                                                    to play a sound and move immediately to the next line of code
values: a note of 60 (equivalent to middle C) for a duration of
                                                                    by using app.start_sound().
0.2 seconds. If you enter only one number, it will be treated
as the first parameter (pitch), and the default duration will be    light matrix
used. For example, hub.speaker.beep(67) will play the note G
                                                                    The Hub’s light matrix can be programmed to display a
for 0.2 seconds.
                                                                    smiley face:
     The beep() method halts the program until the beep has
finished playing. To start playing a beep and move immediately      hub.light_matrix.show_image('HAPPY')
to the next line of code, use hub.speaker.start_beep() instead.
This method is useful if you want to do two things simultane-            Many other images can be displayed—simply replace
ously, such as playing a sound while displaying an image. To        HAPPY with the name of your desired image. Go to HubLight
stop a beep, use hub.speaker.stop().                                Matrix in the Help Center for a list of options.




                                                          	introduction to Python programming                                            3
                                                                               By stringing together multiple set_pixel() commands,
        THINK ABOUT IT                                                     you can create your own patterns on the light matrix. For
                                                                           example, these lines of code will light up the entire middle
        If you run the following two pieces of code, will the              column:
        results be the same? If not, what will be different?
                                                                           hub.light_matrix.set_pixel(2, 0)
        app.play_sound('Emotional Piano')                                  hub.light_matrix.set_pixel(2, 1)
        hub.light_matrix.show_image('HEART')                               hub.light_matrix.set_pixel(2, 2)
                                                                           hub.light_matrix.set_pixel(2, 3)
        app.start_sound('Emotional Piano')                                 hub.light_matrix.set_pixel(2, 4)
        hub.light_matrix.show_image('HEART')
                                                                                 All the commands use 2 as the x-coordinate, correspond-
             Answer: The first version uses the play_sound()               ing to the middle column of the light matrix, but each command
        method, so the entire Emotional Piano sound will                   has a different y-coordinate, so the middle pixel in each row
        play and then a heart will appear on the light matrix.             will be lit up.
        The second version uses start_sound(), so the                            To turn off all the pixels in the light matrix, use this:
        program will play the music and display the heart
        simultaneously.                                                    hub.light_matrix.off()




         To light up individual squares, or pixels, on the light matrix,
    use this:                                                                  TRY THIS
    hub.light_matrix.set_pixel(x, y)                                           Write a program to light the bottom row of the light
                                                                               matrix.
          The x-coordinate defines the pixel’s column, and the
                                                                                   Here’s one possible solution:
    y-coordinate defines the pixel’s row. Rows and columns are
    numbered from 0 to 4, as shown in Figure 1-2. For example,                 hub.light_matrix.set_pixel(0, 4)
    set_pixel(0, 0) lights up the pixel in the upper-left corner of            hub.light_matrix.set_pixel(1, 4)
    the light matrix, and set_pixel(4, 4) lights up the pixel in the           hub.light_matrix.set_pixel(2, 4)
    lower-right corner.                                                        hub.light_matrix.set_pixel(3, 4)
                                                                               hub.light_matrix.set_pixel(4, 4)
     N O T E This numbering system differs from the one used
    in Word Blocks programming, which numbers the light
    matrix’s rows and columns from 1 to 5. As you learn more
    about Python, you’ll find that it’s common in text-based                     The light matrix can scroll words and numbers as well as
    programming to start counting from 0 rather than 1.                    display static images. The method write() scrolls whatever is
                                                                           in its parentheses on the light matrix. If the text is in quota-
                            Columns                                        tion marks, it will be displayed exactly as you type it (minus
                                                                           the quotation marks). For example, this line of code scrolls the
                 0      1      2      3   4
                                                                           words Hello there:
             0
                                                                           hub.light_matrix.write('Hello there')
             1

    Rows     2                                                                  The sequence of characters in quotes is called a string.
                                                                           We’ve already used strings in this chapter to select sound and
             3                                                             image files. You’ll learn more about strings in later chapters,
             4                                                             where you’ll also learn how to display sensor readings and
                                                                           other information on the light matrix without having to type it
    Figure 1-2: In Python programming, the                                 out exactly.
    rows and columns of the light matrix are
    numbered from 0 to 4.




4           CHAPTER 1
status light                                                           Hub buttons
The status light is the round center button on the front of            The Hub’s left and right buttons function as inputs in your pro-
the Hub. In Word Blocks programming, it’s referred to as the           grams. You can use these inputs in several ways. For example,
center button light. To turn on the Hub’s status light, use this       you can wait until a button is pushed. This code waits until the
command:                                                               left button is pushed and then turns the status light pink:

hub.status_light.on()                                                  hub.left_button.wait_until_pressed()
                                                                       hub.status_light.on('pink')
     If you don’t specify a color in the parentheses, the light will
be white. To choose a different color, enter its name as a string          You can wait until a button is released too. The following
(that is, in quotes) in the parentheses, like this:                    code turns on the pink light when the button is pressed and
                                                                       turns it off when the button is released:
hub.status_light.on('blue')
                                                                       hub.left_button.wait_until_pressed()
     You have 11 colors to choose from: azure, blue, cyan,             hub.status_light.on('pink')
green, orange, pink, red, violet, yellow, white, and black (which      hub.left_button.wait_until_released()
                                                                       hub.status_light.off()
turns off the light). These two lines of code, for example, turn
on a red light and then turn the light off:
                                                                          This way, the light will stay on for as long as you hold
hub.status_light.on('red')                                             down the button.
hub.status_light.on('black')

     If you run the code as it’s written, however, the light will
turn red and then immediately turn off so fast that you won’t              TRY THIS
actually see anything happen. In the next chapter, you’ll learn
how to fix this problem.                                                   Let’s say you want to display a new color of status
     Rather than set the light to black, you can turn off the              light every time you press the left Hub button. Will
status light:                                                              the following program display red, then green, then
                                                                           blue? Try it and see:
hub.status_light.off()
                                                                           hub.status_light.on('red')
                                                                           hub.left_button.wait_until_pressed()
                                                                           hub.status_light.on('green')
                                                                           hub.left_button.wait_until_pressed()
    TRY THIS                                                               hub.status_light.on('blue')

    Try reversing the red and black status light com-                           Answer: This program does not display all three
    mands, like so:                                                        colors. Instead, the status light glows red when you
                                                                           start the program and then goes directly to blue
    hub.status_light.on('black')
    hub.status_light.on('red')                                             when you press the Hub button. You never see the
                                                                           green light. Here’s why: both hub.left_button.wait
         What do you think will happen when you execute                    _until_pressed() commands in the program are
    the program? Run it and see.                                           triggered by your single button press.
         Answer: The status light glows red. The first
    command, which turns off the light, is followed imme-
    diately by the command to turn the light red, so you                   What if you want to use multiple button presses in a pro-
    see only the red light.                                            gram? To make sure that each press of the button triggers only
                                                                       one wait_until_pressed() command, pair each wait_until
                                                                       _pressed() command with a wait_until_released() command.
                                                                       That way, the code will wait for the button to be bumped—both
                                                                       pressed and released.




                                                             	introduction to Python programming                                          5
         For example, the code in the red/green/blue light program       indenting and spacing
    you just tried could be rewritten like this:                         Unlike many programming languages, Python uses indentation,
                                                                         or spaces at the beginning of lines, to indicate to the com-
    hub.status_light.on('red')
    hub.left_button.wait_until_pressed()                                 puter how to run a program. For that reason, getting your
    hub.left_button.wait_until_released()                                indentations correct really matters! You’ll see this in the next
    hub.status_light.on('green')                                         chapter when you start using control structures like loops
    hub.left_button.wait_until_pressed()                                 and conditionals.
    hub.left_button.wait_until_released()                                     While spacing is important at the starts of lines, the spacing
    hub.status_light.on('blue')                                          between elements on the same line doesn’t matter. For example,
                                                                         Python will treat hub.speaker.beep(71,1) and hub.speaker
          This program displays all three light colors, switching col-   .beep(71, 1) as equivalent. Whether or not you have a space
    ors each time you press and release the left Hub button.             between 71 and 1, the program will run the same way. However,
          You can also check whether the left button is pressed          the comma between the parameter values is required.
    currently by using hub.left_button.is_pressed(), or you can
    check whether the button was pressed at any time since you           uppercase and lowercase letters
    last checked its status by using hub.left_button.was_pressed().      Python is case sensitive: it views uppercase and lowercase ver-
    You’ll learn how to incorporate these commands into if state-        sions of a letter as different. So, if you write hub.light_matrix
    ments (conditionals) in the next chapter.                            .show_image('Heart') instead of hub.light_matrix.show_image
          To program with the Hub’s right button instead of the left     ('HEART'), you’ll get an error message. HEART in all caps is a
    button, use right instead of left in your code. For example,         valid image to display on the light matrix, but Heart is not.
    hub.right_button.wait_until_pressed() pauses the program                  Be especially careful about capitalization when you’re
    until the right button is pressed.                                   naming things; Python will treat myHub and myhub as two dif-
                                                                         ferent objects. Python doesn’t allow spaces in the middle of a
     N O T E The Hub’s built-in Accelerometer and Gyro Sensor,
                                                                         name, so capitalization can help make the names you choose
    which together are called the Motion Sensor in Python pro-           more readable, as long as the capitalization you use is con-
    gramming, provide other ways of getting input from the Hub.          sistent. For example, capitalizing the H in myHub helps make it
    We cover programming with the Motion Sensor in Chapter 4.            clear where the first word of the name ends and the second
                                                                         word begins.

                                                                         use comments!
        TRY THIS
                                                                         As you already saw, any line that starts with # is treated as a
        Write a program that displays a heart on the light               comment and isn’t executed. This feature is extremely useful
        matrix and then makes it smaller when the right Hub              for documenting your programs. For example, it helps to leave
        button is pressed.                                               a comment before some lines of code, explaining what that
            Here’s one possible solution:                                code is supposed to do. You might think the code’s purpose is
                                                                         clear as you’re writing it, but if you come back to the code a
        hub.light_matrix.show_image('HEART')                             week later, you might not remember what you were thinking.
        hub.right_button.wait_until_pressed()                                 Comments can also be handy for debugging—testing code
        hub.light_matrix.show_image('HEART_SMALL')                       and fixing mistakes. For example, if you want to temporarily
                                                                         stop a line of code from executing, but you don’t want to have to
                                                                         delete and retype it later, simply turn that line into a comment
                                                                         by placing a # in front of it. If you want to stop multiple lines of

    some programming                                                     code from executing (or if you want to write a long comment),
                                                                         enclose the lines of code in triple quotes ("""), like this:

    basics                                                               """
                                                                         these lines won't execute
                                                                         because they're in triple quotes:
    You’ve gotten your first taste of Python as you’ve learned to
    write simple programs that control the Hub’s inputs and outputs.     hub.speaker.beep(67, 1.0)
                                                                         hub.light_matrix.show_image('HEART')
    Now let’s take a step back and look at some basic rules and best
                                                                         """
    practices for programming with Python.



6          CHAPTER 1
printing to the console
You can display words and numbers in the console at the
                                                                                        PROJECTS
bottom of the coding area by using print(). Enter the text
you want to display inside the parentheses as a string (using        SLEEPING
quotes). For example, this code will display the message Hello       Write a program that displays a sleeping face on the
there in the console:                                                light matrix while playing snoring sounds.
print('Hello there')                                                      Here’s one possible solution:

                                                                     app = App()
     You can also print sensor values and other information to       hub.light_matrix.show_image('ASLEEP')
the console. For example, here’s how to print the Hub’s current      app.play_sound('Snoring')
volume level:

print(hub.speaker.get_volume())                                      TRAFFIC LIGHT

     Notice that hub.speaker.get_volume() isn’t enclosed in          Write a program that switches the status light from
quotation marks. This is because it isn’t a string—it’s a method     green to yellow to red, changing to the next color
of the hub object. When this line of code runs, your program will    when you press a Hub button.
know to look up the current volume level of the Hub, and it will         Here’s one possible solution:
print whatever that number is to the console.
                                                                     hub.status_light.on('green')
     Printing to the console can be helpful in debugging your        hub.left_button.wait_until_pressed()
programs and is often much easier than displaying text on the        hub.left_button.wait_until_released()
Hub’s light matrix. For example, you could print output values       hub.status_light.on('yellow')
from the Distance Sensor to the console to check whether the         hub.left_button.wait_until_pressed()
sensor is detecting an object. You can even combine strings and      hub.left_button.wait_until_released()
methods inside print() statements to make your output easier         hub.status_light.on('red')
to understand. For example, this line of code prints the word
volume followed by the current volume level:                         WALK SIGNAL
print('volume', hub.speaker.get_volume())                            Write a program that displays a green status light
                                                                     until the left Hub button is pressed. Then, the status
      Since volume is enclosed in quotes, it’s treated as a string   light turns red and the light matrix scrolls WALK.
and is printed exactly the way it’s typed, whereas hub.speaker       After WALK finishes scrolling, the status light turns
.get_volume() isn’t in quotes, so it’s treated as a method and       green again.
will result in printing the current volume level. When you mix            Here’s one possible solution:
strings and non-strings inside a print() statement, separate
each element with a comma.                                           hub.status_light.on('green')
                                                                     hub.left_button.wait_until_pressed()


summary
                                                                     hub.status_light.on('red')
                                                                     hub.light_matrix.write('WALK')
                                                                     hub.status_light.on('green')

In this chapter, you learned how to write Python programs to
control the Hub inputs and outputs. You also learned a few
Python programming basics, such as how indents, spaces, and
capitalization are treated, and the value of using comments
and print() statements. In the next chapter, you’ll expand your
knowledge of Python by learning how to use control structures
like loops and conditionals, as well as operators.




                                                             	introduction to Python programming                              7
